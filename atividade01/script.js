function mostrarConvidados() {
    let convidados = ["Felipe", "Lucas", "Ana", "Maria", "João"];

    for (let i = 0; i < convidados.length; i++) {
        alert("Convidado: " + convidados[i]);
    }
}